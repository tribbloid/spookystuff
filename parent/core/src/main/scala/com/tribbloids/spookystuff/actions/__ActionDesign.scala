package com.tribbloids.spookystuff.actions

object __ActionDesign {

  /**
    * the purpose of all actions/instructions is to interact with agent or extract information from it
    *
    * the next architecture should ideally use an imperative/define-by-run Agent API, like PyTorch
    *
    * when returning multiple traces at the same time, try returning TraceSet instead, it has built-in deduplication and
    * can easily avoid returning empty result
    */
}
